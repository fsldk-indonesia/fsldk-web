/** Item menu sidebar CMS, diturunkan dari lk_permission (menuLabel/menuIcon/menuRoute/sortOrder). */
export interface MenuItem {
  menuLabel: string;
  menuIcon: string;
  menuRoute: string;
  sortOrder: number;
}
