/** Info kontak PIC (Penanggung Jawab) QR Code — subset read-only App Settings,
 *  dipakai kartu "Konfirmasi via WhatsApp" di halaman pengajuan. */
export interface QRCodePIC {
  picName: string;
  picWhatsapp: string;
}
