/** Info kontak PIC (Penanggung Jawab) shortlink — subset read-only App
 *  Settings, dipakai kartu "Konfirmasi via WhatsApp" di halaman pengajuan. */
export interface ShortLinkPIC {
  picName: string;
  picWhatsapp: string;
}
